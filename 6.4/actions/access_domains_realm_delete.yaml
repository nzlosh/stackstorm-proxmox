name: access_domains_realm_delete
pack: proxmox
runner_type: python-script
description: "Delete an authentication server."
enabled: true
entry_point: "access_domains_realm_delete.py"
parameters:
  realm:
    description: "Authentication domain ID"
    type: string
    required: true
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
