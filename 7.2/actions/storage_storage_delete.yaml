name: storage_storage_delete
pack: proxmox
runner_type: python-script
description: "Delete storage configuration."
enabled: true
entry_point: "storage_storage_delete.py"
parameters:
  storage:
    description: "The storage identifier."
    type: string
    required: true
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
