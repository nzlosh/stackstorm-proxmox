name: nodes_node_network_revert_network_changes
pack: proxmox
runner_type: python-script
description: "Revert network configuration changes."
enabled: true
entry_point: "nodes_node_network_revert_network_changes.py"
parameters:
  node:
    description: "The cluster node name."
    type: string
    required: true
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
