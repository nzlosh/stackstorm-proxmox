name: nodes_subscription_update
pack: proxmox
runner_type: python-script
description: "Update subscription info."
enabled: true
entry_point: "nodes_subscription_update.py"
parameters:
  force:
    description: "Always connect to server, even if we have up to date info inside local cache."
    default: 0
    type: boolean
    required: false
    secret: false
  node:
    description: "The cluster node name."
    type: string
    required: true
    secret: false
