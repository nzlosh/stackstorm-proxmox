name: nodes_storage_rrddata
pack: proxmox
runner_type: python-script
description: "Read storage RRD statistics."
enabled: true
entry_point: "nodes_storage_rrddata.py"
parameters:
  cf:
    description: "The RRD consolidation function"
    enum:
      - AVERAGE
      - MAX
    type: string
    required: false
  node:
    description: "The cluster node name."
    type: string
    required: true
  storage:
    description: "The storage identifier."
    type: string
    required: true
  timeframe:
    description: "Specify the time frame you are interested in."
    enum:
      - hour
      - day
      - week
      - month
      - year
    type: string
    required: true
