name: nodes_lxc_template
pack: proxmox
runner_type: python-script
description: "Create a Template."
enabled: true
entry_point: "nodes_lxc_template.py"
parameters:
  node:
    description: "The cluster node name."
    type: string
    required: true
  vmid:
    description: "The (unique) ID of the VM."
    type: integer
    required: true
