name: nodes_qemu_agent_get-users
pack: proxmox
runner_type: python-script
description: "Execute get-users."
enabled: true
entry_point: "nodes_qemu_agent_get-users.py"
parameters:
  node:
    description: "The cluster node name."
    type: string
    required: true
  vmid:
    description: "The (unique) ID of the VM."
    type: integer
    required: true
