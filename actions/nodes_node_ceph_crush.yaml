name: nodes_node_ceph_crush
pack: proxmox
runner_type: python-script
description: "Get OSD crush map"
enabled: true
entry_point: "nodes_node_ceph_crush.py"
parameters:
  api_timeout:
    description: "Time in seconds to wait for API response. (Default: 5)"
    type: integer
    required: false
    default: 5
  node:
    description: "The cluster node name."
    type: string
    required: true
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
