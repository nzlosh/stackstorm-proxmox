name: cluster_firewall_aliases_get_aliases
pack: proxmox
runner_type: python-script
description: "List aliases"
enabled: true
entry_point: "cluster_firewall_aliases_get_aliases.py"
parameters:
  api_timeout:
    description: "Time in seconds to wait for API response. (Default: 5)"
    type: integer
    required: false
    default: 5
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
