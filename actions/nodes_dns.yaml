name: nodes_dns
pack: proxmox
runner_type: python-script
description: "Read DNS settings."
enabled: true
entry_point: "nodes_dns.py"
parameters:
  node:
    description: "The cluster node name."
    type: string
    required: true
    secret: false
