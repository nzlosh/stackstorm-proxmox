name: cluster_status_get_status
pack: proxmox
runner_type: python-script
description: "Get cluster status information."
enabled: true
entry_point: "cluster_status_get_status.py"
parameters:
  api_timeout:
    description: "Time in seconds to wait for API response. (Default: 5)"
    type: integer
    required: false
    default: 5
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
