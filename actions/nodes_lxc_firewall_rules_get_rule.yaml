name: nodes_lxc_firewall_rules_get_rule
pack: proxmox
runner_type: python-script
description: "Get single rule data."
enabled: true
entry_point: "nodes_lxc_firewall_rules_get_rule.py"
parameters:
  node:
    description: "The cluster node name."
    type: string
    required: true
    secret: false
  pos:
    description: "Update rule at position <pos>."
    type: integer
    required: false
    secret: false
  vmid:
    description: "The (unique) ID of the VM."
    type: integer
    required: true
    secret: false
