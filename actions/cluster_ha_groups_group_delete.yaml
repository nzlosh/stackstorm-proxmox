name: cluster_ha_groups_group_delete
pack: proxmox
runner_type: python-script
description: "Delete ha group configuration."
enabled: true
entry_point: "cluster_ha_groups_group_delete.py"
parameters:
  group:
    description: "The HA group identifier."
    type: string
    required: true
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
