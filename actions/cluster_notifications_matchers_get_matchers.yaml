name: cluster_notifications_matchers_get_matchers
pack: proxmox
runner_type: python-script
description: "Returns a list of all matchers"
enabled: true
entry_point: "cluster_notifications_matchers_get_matchers.py"
parameters:
  api_timeout:
    description: "Time in seconds to wait for API response. (Default: 5)"
    type: integer
    required: false
    default: 5
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
