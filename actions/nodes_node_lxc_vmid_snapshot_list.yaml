name: nodes_node_lxc_vmid_snapshot_list
pack: proxmox
runner_type: python-script
description: "List all snapshots."
enabled: true
entry_point: "nodes_node_lxc_vmid_snapshot_list.py"
parameters:
  api_timeout:
    description: "Time in seconds to wait for API response. (Default: 5)"
    type: integer
    required: false
    default: 5
  node:
    description: "The cluster node name."
    type: string
    required: true
  vmid:
    description: "The (unique) ID of the VM."
    type: integer
    required: true
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
