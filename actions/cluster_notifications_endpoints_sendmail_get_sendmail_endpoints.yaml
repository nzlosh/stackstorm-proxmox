name: cluster_notifications_endpoints_sendmail_get_sendmail_endpoints
pack: proxmox
runner_type: python-script
description: "Returns a list of all sendmail endpoints"
enabled: true
entry_point: "cluster_notifications_endpoints_sendmail_get_sendmail_endpoints.py"
parameters:
  api_timeout:
    description: "Time in seconds to wait for API response. (Default: 5)"
    type: integer
    required: false
    default: 5
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
