name: access_groups_create_group
pack: proxmox
runner_type: python-script
description: "Create new group."
enabled: true
entry_point: "access_groups_create_group.py"
parameters:
  api_timeout:
    description: "Time in seconds to wait for API response. (Default: 5)"
    type: integer
    required: false
    default: 5
  comment:
    description: "Description unavailable."
    type: string
    required: false
  groupid:
    description: "Description unavailable."
    type: string
    required: true
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
