name: nodes_ceph_stop
pack: proxmox
runner_type: python-script
description: "Stop ceph services."
enabled: true
entry_point: "nodes_ceph_stop.py"
parameters:
  node:
    description: "The cluster node name."
    type: string
    required: true
  service:
    description: "Ceph service name."
    default: "ceph.target"
    type: string
    required: false
