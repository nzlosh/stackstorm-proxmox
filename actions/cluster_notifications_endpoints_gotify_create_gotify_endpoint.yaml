name: cluster_notifications_endpoints_gotify_create_gotify_endpoint
pack: proxmox
runner_type: python-script
description: "Create a new gotify endpoint"
enabled: true
entry_point: "cluster_notifications_endpoints_gotify_create_gotify_endpoint.py"
parameters:
  api_timeout:
    description: "Time in seconds to wait for API response. (Default: 5)"
    type: integer
    required: false
    default: 5
  comment:
    description: "Comment"
    type: string
    required: false
  disable:
    description: "Disable this target"
    type: boolean
    required: false
  name:
    description: "The name of the endpoint."
    type: string
    required: true
  server:
    description: "Server URL"
    type: string
    required: true
  token:
    description: "Secret token"
    type: string
    required: true
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
