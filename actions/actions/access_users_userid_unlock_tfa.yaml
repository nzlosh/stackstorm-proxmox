name: access_users_userid_unlock_tfa
pack: proxmox
runner_type: python-script
description: "Unlock a user's TFA authentication."
enabled: true
entry_point: "access_users_userid_unlock_tfa.py"
parameters:
  userid:
    description: "Full User ID, in the `name@realm` format."
    type: string
    required: true
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
