name: nodes_node_apt_repositories
pack: proxmox
runner_type: python-script
description: "Get APT repository information."
enabled: true
entry_point: "nodes_node_apt_repositories.py"
parameters:
  node:
    description: "The cluster node name."
    type: string
    required: true
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
