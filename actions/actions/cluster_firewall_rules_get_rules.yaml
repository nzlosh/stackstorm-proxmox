name: cluster_firewall_rules_get_rules
pack: proxmox
runner_type: python-script
description: "List rules."
enabled: true
entry_point: "cluster_firewall_rules_get_rules.py"
parameters:
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
