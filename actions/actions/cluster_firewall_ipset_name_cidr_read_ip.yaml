name: cluster_firewall_ipset_name_cidr_read_ip
pack: proxmox
runner_type: python-script
description: "Read IP or Network settings from IPSet."
enabled: true
entry_point: "cluster_firewall_ipset_name_cidr_read_ip.py"
parameters:
  cidr:
    description: "Network/IP specification in CIDR format."
    type: string
    required: true
  name:
    description: "IP set name."
    type: string
    required: true
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
