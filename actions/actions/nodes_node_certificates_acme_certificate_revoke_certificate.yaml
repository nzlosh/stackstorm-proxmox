name: nodes_node_certificates_acme_certificate_revoke_certificate
pack: proxmox
runner_type: python-script
description: "Revoke existing certificate from CA."
enabled: true
entry_point: "nodes_node_certificates_acme_certificate_revoke_certificate.py"
parameters:
  node:
    description: "The cluster node name."
    type: string
    required: true
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
