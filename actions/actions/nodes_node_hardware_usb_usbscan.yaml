name: nodes_node_hardware_usb_usbscan
pack: proxmox
runner_type: python-script
description: "List local USB devices."
enabled: true
entry_point: "nodes_node_hardware_usb_usbscan.py"
parameters:
  node:
    description: "The cluster node name."
    type: string
    required: true
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
