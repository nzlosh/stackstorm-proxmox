name: cluster_replication_index
pack: proxmox
runner_type: python-script
description: "List replication jobs."
enabled: true
entry_point: "cluster_replication_index.py"
parameters:
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
