name: cluster_notifications_endpoints_sendmail_name_delete_sendmail_endpoint
pack: proxmox
runner_type: python-script
description: "Remove sendmail endpoint"
enabled: true
entry_point: "cluster_notifications_endpoints_sendmail_name_delete_sendmail_endpoint.py"
parameters:
  api_timeout:
    description: "Time in seconds to wait for API response. (Default: 5)"
    type: integer
    required: false
    default: 5
  name:
    description: "Description unavailable."
    type: string
    required: true
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
