name: cluster_metrics_server_id_delete
pack: proxmox
runner_type: python-script
description: "Remove Metric server."
enabled: true
entry_point: "cluster_metrics_server_id_delete.py"
parameters:
  api_timeout:
    description: "Time in seconds to wait for API response. (Default: 5)"
    type: integer
    required: false
    default: 5
  prox_id:
    description: "Description unavailable."
    type: string
    required: true
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
