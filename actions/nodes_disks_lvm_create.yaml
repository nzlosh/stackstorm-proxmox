name: nodes_disks_lvm_create
pack: proxmox
runner_type: python-script
description: "Create an LVM Volume Group"
enabled: true
entry_point: "nodes_disks_lvm_create.py"
parameters:
  add_storage:
    description: "Configure storage using the Volume Group"
    default: 0
    type: boolean
    required: false
    secret: false
  device:
    description: "The block device you want to create the volume group on"
    type: string
    required: true
    secret: false
  name:
    description: "The storage identifier."
    type: string
    required: true
    secret: false
  node:
    description: "The cluster node name."
    type: string
    required: true
    secret: false
