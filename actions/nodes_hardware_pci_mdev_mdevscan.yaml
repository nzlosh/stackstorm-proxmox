name: nodes_hardware_pci_mdev_mdevscan
pack: proxmox
runner_type: python-script
description: "List mediated device types for given PCI device."
enabled: true
entry_point: "nodes_hardware_pci_mdev_mdevscan.py"
parameters:
  node:
    description: "The cluster node name."
    type: string
    required: true
    secret: false
  pciid:
    description: "The PCI ID to list the mdev types for."
    type: string
    required: true
    secret: false
