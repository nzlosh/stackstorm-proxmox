name: cluster_ceph_metadata
pack: proxmox
runner_type: python-script
description: "Get ceph metadata."
enabled: true
entry_point: "cluster_ceph_metadata.py"
parameters:
  api_timeout:
    description: "Time in seconds to wait for API response. (Default: 5)"
    type: integer
    required: false
    default: 5
  scope:
    description: "Description unavailable."
    type: string
    required: false
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
