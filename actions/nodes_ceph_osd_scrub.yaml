name: nodes_ceph_osd_scrub
pack: proxmox
runner_type: python-script
description: "Instruct the OSD to scrub."
enabled: true
entry_point: "nodes_ceph_osd_scrub.py"
parameters:
  deep:
    description: "If set, instructs a deep scrub instead of a normal one."
    default: 0
    type: boolean
    required: false
  node:
    description: "The cluster node name."
    type: string
    required: true
  osdid:
    description: "OSD ID"
    type: integer
    required: true
