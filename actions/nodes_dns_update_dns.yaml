name: nodes_dns_update_dns
pack: proxmox
runner_type: python-script
description: "Write DNS settings."
enabled: true
entry_point: "nodes_dns_update_dns.py"
parameters:
  dns1:
    description: "First name server IP address."
    type: string
    required: false
    secret: false
  dns2:
    description: "Second name server IP address."
    type: string
    required: false
    secret: false
  dns3:
    description: "Third name server IP address."
    type: string
    required: false
    secret: false
  node:
    description: "The cluster node name."
    type: string
    required: true
    secret: false
  search:
    description: "Search domain for host-name lookup."
    type: string
    required: true
    secret: false
