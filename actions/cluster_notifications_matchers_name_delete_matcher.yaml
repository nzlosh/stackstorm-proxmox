name: cluster_notifications_matchers_name_delete_matcher
pack: proxmox
runner_type: python-script
description: "Remove matcher"
enabled: true
entry_point: "cluster_notifications_matchers_name_delete_matcher.py"
parameters:
  api_timeout:
    description: "Time in seconds to wait for API response. (Default: 5)"
    type: integer
    required: false
    default: 5
  name:
    description: "Description unavailable."
    type: string
    required: true
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
