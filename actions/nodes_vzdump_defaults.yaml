name: nodes_vzdump_defaults
pack: proxmox
runner_type: python-script
description: "Get the currently configured vzdump defaults."
enabled: true
entry_point: "nodes_vzdump_defaults.py"
parameters:
  node:
    description: "The cluster node name."
    type: string
    required: true
    secret: false
  storage:
    description: "The storage identifier."
    type: string
    required: false
    secret: false
