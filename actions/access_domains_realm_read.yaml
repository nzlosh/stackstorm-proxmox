name: access_domains_realm_read
pack: proxmox
runner_type: python-script
description: "Get auth server configuration."
enabled: true
entry_point: "access_domains_realm_read.py"
parameters:
  api_timeout:
    description: "Time in seconds to wait for API response. (Default: 5)"
    type: integer
    required: false
    default: 5
  realm:
    description: "Authentication domain ID"
    type: string
    required: true
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
