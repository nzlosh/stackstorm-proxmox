name: storage_read
pack: proxmox
runner_type: python-script
description: "Read storage configuration."
enabled: true
entry_point: "storage_read.py"
parameters:
  storage:
    description: "The storage identifier."
    type: string
    required: true
