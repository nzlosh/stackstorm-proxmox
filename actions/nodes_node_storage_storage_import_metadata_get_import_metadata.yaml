name: nodes_node_storage_storage_import_metadata_get_import_metadata
pack: proxmox
runner_type: python-script
description: "Get the base parameters for creating a guest which imports data from a foreign importable guest, like an ESXi VM"
enabled: true
entry_point: "nodes_node_storage_storage_import_metadata_get_import_metadata.py"
parameters:
  api_timeout:
    description: "Time in seconds to wait for API response. (Default: 5)"
    type: integer
    required: false
    default: 5
  node:
    description: "The cluster node name."
    type: string
    required: true
  storage:
    description: "The storage identifier."
    type: string
    required: true
  volume:
    description: "Volume identifier for the guest archive/entry."
    type: string
    required: true
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
