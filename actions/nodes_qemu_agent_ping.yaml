name: nodes_qemu_agent_ping
pack: proxmox
runner_type: python-script
description: "Execute ping."
enabled: true
entry_point: "nodes_qemu_agent_ping.py"
parameters:
  node:
    description: "The cluster node name."
    type: string
    required: true
    secret: false
  vmid:
    description: "The (unique) ID of the VM."
    type: integer
    required: true
    secret: false
