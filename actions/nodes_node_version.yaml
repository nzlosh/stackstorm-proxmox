name: nodes_node_version
pack: proxmox
runner_type: python-script
description: "API version details"
enabled: true
entry_point: "nodes_node_version.py"
parameters:
  node:
    description: "The cluster node name."
    type: string
    required: true
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
