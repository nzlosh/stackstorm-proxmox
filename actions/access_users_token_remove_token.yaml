name: access_users_token_remove_token
pack: proxmox
runner_type: python-script
description: "Remove API token for a specific user."
enabled: true
entry_point: "access_users_token_remove_token.py"
parameters:
  tokenid:
    description: "User-specific token identifier."
    type: string
    required: true
    secret: false
  userid:
    description: "User ID"
    type: string
    required: true
    secret: false
