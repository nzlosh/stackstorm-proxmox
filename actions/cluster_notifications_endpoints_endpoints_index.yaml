name: cluster_notifications_endpoints_endpoints_index
pack: proxmox
runner_type: python-script
description: "Index for all available endpoint types."
enabled: true
entry_point: "cluster_notifications_endpoints_endpoints_index.py"
parameters:
  api_timeout:
    description: "Time in seconds to wait for API response. (Default: 5)"
    type: integer
    required: false
    default: 5
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
