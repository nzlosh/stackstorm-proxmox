name: cluster_ha_groups_group_read
pack: proxmox
runner_type: python-script
description: "Read ha group configuration."
enabled: true
entry_point: "cluster_ha_groups_group_read.py"
parameters:
  api_timeout:
    description: "Time in seconds to wait for API response. (Default: 5)"
    type: integer
    required: false
    default: 5
  group:
    description: "The HA group identifier."
    type: string
    required: true
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
