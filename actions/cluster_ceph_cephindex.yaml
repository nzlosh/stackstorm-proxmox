name: cluster_ceph_cephindex
pack: proxmox
runner_type: python-script
description: "Cluster ceph index."
enabled: true
entry_point: "cluster_ceph_cephindex.py"
parameters:
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
