name: nodes_execute
pack: proxmox
runner_type: python-script
description: "Execute multiple commands in order."
enabled: true
entry_point: "nodes_execute.py"
parameters:
  commands:
    description: "JSON encoded array of commands."
    type: string
    required: true
  node:
    description: "The cluster node name."
    type: string
    required: true
