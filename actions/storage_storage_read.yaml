name: storage_storage_read
pack: proxmox
runner_type: python-script
description: "Read storage configuration."
enabled: true
entry_point: "storage_storage_read.py"
parameters:
  api_timeout:
    description: "Time in seconds to wait for API response. (Default: 5)"
    type: integer
    required: false
    default: 5
  storage:
    description: "The storage identifier."
    type: string
    required: true
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
