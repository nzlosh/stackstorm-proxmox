name: pools_poolid_delete_pool
pack: proxmox
runner_type: python-script
description: "Delete pool."
enabled: true
entry_point: "pools_poolid_delete_pool.py"
parameters:
  poolid:
    description: "Description unavailable."
    type: string
    required: true
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
