name: nodes_wakeonlan
pack: proxmox
runner_type: python-script
description: "Try to wake a node via 'wake on LAN' network packet."
enabled: true
entry_point: "nodes_wakeonlan.py"
parameters:
  node:
    description: "target node for wake on LAN packet"
    type: string
    required: true
    secret: false
