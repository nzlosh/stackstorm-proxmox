name: cluster_mapping_pci_id_get
pack: proxmox
runner_type: python-script
description: "Get PCI Mapping."
enabled: true
entry_point: "cluster_mapping_pci_id_get.py"
parameters:
  api_timeout:
    description: "Time in seconds to wait for API response. (Default: 5)"
    type: integer
    required: false
    default: 5
  prox_id:
    description: "Description unavailable."
    type: string
    required: true
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
