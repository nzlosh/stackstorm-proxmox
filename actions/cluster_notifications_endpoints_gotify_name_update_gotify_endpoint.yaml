name: cluster_notifications_endpoints_gotify_name_update_gotify_endpoint
pack: proxmox
runner_type: python-script
description: "Update existing gotify endpoint"
enabled: true
entry_point: "cluster_notifications_endpoints_gotify_name_update_gotify_endpoint.py"
parameters:
  api_timeout:
    description: "Time in seconds to wait for API response. (Default: 5)"
    type: integer
    required: false
    default: 5
  comment:
    description: "Comment"
    type: string
    required: false
  delete:
    description: "A list of settings you want to delete."
    type: array
    required: false
  digest:
    description: "Prevent changes if current configuration file has a different digest. This can be used to prevent concurrent modifications."
    type: string
    required: false
  disable:
    description: "Disable this target"
    type: boolean
    required: false
  name:
    description: "The name of the endpoint."
    type: string
    required: true
  server:
    description: "Server URL"
    type: string
    required: false
  token:
    description: "Secret token"
    type: string
    required: false
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
