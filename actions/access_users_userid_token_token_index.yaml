name: access_users_userid_token_token_index
pack: proxmox
runner_type: python-script
description: "Get user API tokens."
enabled: true
entry_point: "access_users_userid_token_token_index.py"
parameters:
  userid:
    description: "Full User ID, in the `name@realm` format."
    type: string
    required: true
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
