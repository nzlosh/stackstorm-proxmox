name: storage_delete
pack: proxmox
runner_type: python-script
description: "Delete storage configuration."
enabled: true
entry_point: "storage_delete.py"
parameters:
  storage:
    description: "The storage identifier."
    type: string
    required: true
