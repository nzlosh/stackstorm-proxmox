name: pools_create_pool
pack: proxmox
runner_type: python-script
description: "Create new pool."
enabled: true
entry_point: "pools_create_pool.py"
parameters:
  comment:
    description: "Description unavailable."
    type: string
    required: false
  poolid:
    description: "Description unavailable."
    type: string
    required: true
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
