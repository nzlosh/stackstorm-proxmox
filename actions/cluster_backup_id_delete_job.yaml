name: cluster_backup_id_delete_job
pack: proxmox
runner_type: python-script
description: "Delete vzdump backup job definition."
enabled: true
entry_point: "cluster_backup_id_delete_job.py"
parameters:
  api_timeout:
    description: "Time in seconds to wait for API response. (Default: 5)"
    type: integer
    required: false
    default: 5
  prox_id:
    description: "The job ID."
    type: string
    required: true
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
