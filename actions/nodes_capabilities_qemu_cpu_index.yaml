name: nodes_capabilities_qemu_cpu_index
pack: proxmox
runner_type: python-script
description: "List all custom and default CPU models."
enabled: true
entry_point: "nodes_capabilities_qemu_cpu_index.py"
parameters:
  node:
    description: "The cluster node name."
    type: string
    required: true
    secret: false
