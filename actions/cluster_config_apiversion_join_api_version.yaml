name: cluster_config_apiversion_join_api_version
pack: proxmox
runner_type: python-script
description: "Return the version of the cluster join API available on this node."
enabled: true
entry_point: "cluster_config_apiversion_join_api_version.py"
parameters:
  api_timeout:
    description: "Time in seconds to wait for API response. (Default: 5)"
    type: integer
    required: false
    default: 5
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
