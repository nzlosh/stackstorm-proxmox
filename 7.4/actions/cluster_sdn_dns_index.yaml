name: cluster_sdn_dns_index
pack: proxmox
runner_type: python-script
description: "SDN dns index."
enabled: true
entry_point: "cluster_sdn_dns_index.py"
parameters:
  prox_type:
    description: "Only list sdn dns of specific type"
    type: string
    required: false
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
