name: cluster_backup_index
pack: proxmox
runner_type: python-script
description: "List vzdump backup schedule."
enabled: true
entry_point: "cluster_backup_index.py"
parameters:
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
