name: nodes_index
pack: proxmox
runner_type: python-script
description: "Cluster node index."
enabled: true
entry_point: "nodes_index.py"
parameters:
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
