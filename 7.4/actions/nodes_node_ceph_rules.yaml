name: nodes_node_ceph_rules
pack: proxmox
runner_type: python-script
description: "List ceph rules."
enabled: true
entry_point: "nodes_node_ceph_rules.py"
parameters:
  node:
    description: "The cluster node name."
    type: string
    required: true
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
