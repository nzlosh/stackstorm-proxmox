name: nodes_node_hosts_get_etc_hosts
pack: proxmox
runner_type: python-script
description: "Get the content of /etc/hosts."
enabled: true
entry_point: "nodes_node_hosts_get_etc_hosts.py"
parameters:
  node:
    description: "The cluster node name."
    type: string
    required: true
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
