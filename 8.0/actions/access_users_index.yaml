name: access_users_index
pack: proxmox
runner_type: python-script
description: "User index."
enabled: true
entry_point: "access_users_index.py"
parameters:
  enabled:
    description: "Optional filter for enable property."
    type: boolean
    required: false
  full:
    description: "Include group and token information."
    type: boolean
    required: false
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
