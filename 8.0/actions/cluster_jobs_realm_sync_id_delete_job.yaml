name: cluster_jobs_realm_sync_id_delete_job
pack: proxmox
runner_type: python-script
description: "Delete realm-sync job definition."
enabled: true
entry_point: "cluster_jobs_realm_sync_id_delete_job.py"
parameters:
  prox_id:
    description: "Description unavailable."
    type: string
    required: true
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
