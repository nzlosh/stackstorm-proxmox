name: cluster_mapping_usb_index
pack: proxmox
runner_type: python-script
description: "List USB Hardware Mappings"
enabled: true
entry_point: "cluster_mapping_usb_index.py"
parameters:
  check_node:
    description: "If given, checks the configurations on the given node for correctness, and adds relevant errors to the devices."
    type: string
    required: false
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
