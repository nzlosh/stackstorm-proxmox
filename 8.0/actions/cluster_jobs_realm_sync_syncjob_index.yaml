name: cluster_jobs_realm_sync_syncjob_index
pack: proxmox
runner_type: python-script
description: "List configured realm-sync-jobs."
enabled: true
entry_point: "cluster_jobs_realm_sync_syncjob_index.py"
parameters:
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
