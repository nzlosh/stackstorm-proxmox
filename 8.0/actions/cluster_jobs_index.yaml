name: cluster_jobs_index
pack: proxmox
runner_type: python-script
description: "Index for jobs related endpoints."
enabled: true
entry_point: "cluster_jobs_index.py"
parameters:
  profile_name:
    description: "The profile to use to run the action."
    type: string
    required: false
